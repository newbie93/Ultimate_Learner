#include<bits/stdc++.h>

using namespace std;

int main()
{
    int n,m,i,a,b,c;
    cin>>n>>m;
    priority_queue < pair <int,int> , vector <int,int> , greater <int,int> () > g[n+1];
    for(i=1;i<=m;i++)
    {
        cin>>a>>b>>c;
        g[a].push_back(make_pair())
    }
    return 0;
}
